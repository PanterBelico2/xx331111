// Ejemplo de implementación de un lightbox simple
document.querySelectorAll('[data-lightbox]').forEach(function(element) {
    element.addEventListener('click', function(event) {
        event.preventDefault();
        // Código para mostrar la imagen en una ventana modal
        // Implementación personalizada o uso de una librería
    });
});
